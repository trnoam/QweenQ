<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_employee_password"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.qween.qweenq.EmployeePasswordActivity"
    tools:showIn="@layout/activity_employee_password"
    android:minHeight="50dp"
    android:orientation="vertical">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:id="@+id/spinners_layout">

        <Spinner
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:id="@+id/adjective_employee"
            android:layout_weight="1" />

        <Spinner
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:id="@+id/noun_employee"
            android:layout_weight="1" />

    </LinearLayout>

    <Button
        android:text="Generate random password"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:id="@+id/random_password"
        android:minHeight="50dp"
        android:height="50dp"
        android:textColor="@color/colorToolBar"
        android:background="?attr/selectableItemBackground"
        android:textSize="20sp"
        android:onClick="on_random_password_clicked" />

</LinearLayout>
